Content-Type: text/plain; charset=utf-8

Dobrý den.
Našli jsme nové záznamy pro hlídače nových údajů "@Model.WDName".

V době @Model.Interval @Model.TotalTxt z datové sady "@Model.Dataset.Registration().name" pro tento dotaz
@Model.Query

@if (Model.AddResults > 0)
{
    <text>
        Zobrazujeme prvních 30 záznamů
    </text>
}

@foreach (dynamic item in @Model.Results)
{

    <text>#ITEMTEMPLATE#</text>

}

Více: @Raw(Model.SearchQuery)?utm_source=hlidac&utm_medium=emailtxt&utm_campaign=detail
============================================


@if (Model.AddResults > 0)
{
    <text>
        Zobrazit všechny nalezené záznamy: @Raw(Model.SearchQuery)&utm_source=hlidac&utm_medium=emailtxt&utm_campaign=more
    </text>
}



@Raw(Model.EmailFooterText)

